4.1
	手段を選ばず。

4.2
	たとえば、[a〜z 0〜9」を表示しようとしたとき、文字数が多すぎるので途中までしか表示されないはず。

4.3
	左端に全て表示されてからスクロール。
	文字列は15文字以下。	無限ループ
	上の行だけ。
	前画面クリアを使うのは不可。

4.4
	2行に渡る。開始位置は任意。
	右端からはみ出した奴は一文字づつひだりから現れる

4.5
	表示しようとする値は、数値としてプログラムに与える。
	(文字列で与えてはいけない)

4.6
	a,b,演算子はプログラムに書いておく(後からキー入力というわけではない)
